---
description: Research phase — investigate before any code is written (Aspen pipeline step 1)
---

You are in the **RESEARCH** phase for: $ARGUMENTS

Do NOT write implementation code. Your job is to understand and report.

1. Read the relevant spec in `docs/` (find the right one via `docs/00_INDEX.md`).
2. Re-read the non-negotiables in `CLAUDE.md`. Note any that this work touches.
3. Search the existing codebase for related patterns, types, or prior art. Prefer reusing what exists.
4. If external libraries/APIs are involved, check current docs (use ECC's research/search skills, e.g. `search-first` / `documentation-lookup` / `research-ops`, per your install).
5. Identify unknowns, risks, and anything that needs a human or advisor decision.

**Output:** a short findings report — what the spec requires, what already exists, what the options are, what's risky, and what (if anything) blocks planning. End by recommending whether to proceed to `/plan`.

Stop there. No code, no file edits.
