<?xml version="1.0" encoding="utf-8"?>
<!-- DEBUG ONLY: allows plain http to the local dev server (10.0.2.2). Release builds keep the
     platform default (cleartext blocked); no production endpoint exists yet (Phase 6.9). -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application android:usesCleartextTraffic="true" />
</manifest>
